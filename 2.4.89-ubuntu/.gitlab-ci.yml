build 2.4.89-ubuntu:
  extends: .build
  variables:
    VERSION: "2.4.89-ubuntu"
    TAGS: "${VERSION}-dev"
    RELEASE_DATE: 2018-06

test 2.4.89-ubuntu:
  extends: .test
  variables:
    VERSION: "2.4.89-ubuntu"
  only:
    changes:
    - 2.4.89-ubuntu/*